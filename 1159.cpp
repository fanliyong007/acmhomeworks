#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
int main()
{
    string X, Y;
    while(cin>>X>>Y)
    {
        int max = 0, tmp = 0,flag=1;
        int len1 = X.length(),len2=Y.length();
        for (int i = 1; i <= len1;i++)
        {
            for (int j = flag; j <= len2;j++)
            {
             
            }
        }
    }
    return 0;
}


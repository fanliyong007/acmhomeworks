#include<bits/stdc++.h>
using namespace std;
int gcd(int a,int b)
{
    int tmp = 0;
    while(b!=0)
    {
        
    }
}
int main()
{
    int num[51];
    int n = 0;
    
    while(scanf("%d", &n)!=EOF)
    {
        memset(num, 0, sizeof(num));
        if(n==0)
            break;
        for (int i = 0; i < n;i++)
        {

        }
    }
    return 0;
}